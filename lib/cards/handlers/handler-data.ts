
/**
 * Currently unused class that contains data that is passed to the handlers every time they make their turn
 * It allows the handlers to be stateless, and may contain an internal object that players may set their own data in
 */
export class HandlerData {

}